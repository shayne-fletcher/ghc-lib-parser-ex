resolver: lts-15.5
extra-deps: [ghc-lib-parser-0.20200401]
ghc-options:
    "$locals": -Wall -Wno-name-shadowing
flags:
  ghc-lib-parser-ex:
    auto: false
    no-ghc-lib: false

# Packages MUST go at the end, since we append to it during execution
# of CI.hs.
packages:
- .
